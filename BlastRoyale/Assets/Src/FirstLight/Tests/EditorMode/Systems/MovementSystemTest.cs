using NUnit.Framework;
using Photon.Deterministic;
using Quantum.Systems;
using Assert = NUnit.Framework.Assert;

namespace FirstLight.Tests.EditorMode.Systems
{
	/*public unsafe class MovementSystemTest : UnsafeBaseTestFixture<AiSystem.AiFilter>
	{
		private AiSystem _aiSystem;

		[SetUp]
		public void Init()
		{
			_aiSystem = new AiSystem();
			
			TestData.Transform->Position = FPVector3.Zero;
			TestData.Transform->Rotation = FPQuaternion.Identity;
		}

		//[Test] 
		public void UpdateCheck()
		{
			_aiSystem.Update(Frame, ref TestData);
			
			//Assert.AreEqual(FPVector3.One.XY.XOY * DeltaTime, TestData.Transform->Position);
		}
	}*/
}